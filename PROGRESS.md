# Cobweb 구현 현황과 남은 작업

검증일: 2026-09-17. 운영 완성본이 아닌, Python 문제 풀이 MVP 기준입니다.
현재 현황 문서는 이 파일 하나로 관리합니다. 과거 진행 문서는 `.local/archive/`에 보관했으며 실제 사용자 DB는 변경하지 않았습니다.

| 항목 | 구현 | 검증 |
|---|---|---|
| 1. 마이그레이션 | Flyway V1 스키마, V2 번호 발급·채점 큐 | 별도 MySQL 8.4 DB에 적용 |
| 2. 개발 DB | 3307 포트 Docker Compose, healthcheck | Compose 구성 검사, MySQL 통합 테스트 |
| 3. 문제 조회 | 공개 목록·검색·페이징·상세, 비공개 접근 제한 | HTTP 조회·권한 테스트 및 브라우저 확인 |
| 4. 작성·검수 | 초안 작성/수정, 검수 요청, 운영자 승인·반려 | 상태 전환·권한·동시 번호 발급 테스트 |
| 5. 제출 | Python 제출·본인 목록·결과 API, DB 큐 | HTTP 제출 및 결과 조회 테스트 |
| 6. 체인 레이저 | 1000번 FUNCTION 채점, 비공개 케이스 10개 | HTTP 제출 → 실제 Docker → 10개 정답 |
| 7. 실행 격리 | 네트워크 차단, 읽기 전용, 자원·출력 제한 | 무한 루프·대량 출력·파일 쓰기·네트워크·입력 정체 테스트 |
| 8. 통계·프로필 | 중복 해결 방지, 랭킹, 프로필 조회·수정 | 중복 결과 및 해결 수·프로필 검증 |
| 9. 화면 | index.html 문제/제출/인증/작성/검수/랭킹/프로필 | 브라우저 문제 목록·본문·제출 접근 제어 확인 |
| 10. CI·배포 | GitHub Actions, 앱/워커 이미지, 운영 Compose, Caddy | 앱 이미지 빌드와 개발·운영 Compose 구성 검사 |

## 실행한 검증

- `DOCKER_JUDGE_TEST=true ./gradlew clean build`: 38개 테스트, 실패 0, 건너뜀 0.
- Docker 채점 테스트 8개, MVP 통합 테스트 18개, 인증/토큰 동시성 테스트 3개, 컨텍스트/서비스 테스트 9개.
- `/`, `/index.html`, 기존 `/app.html`의 동일 화면 응답과 중복 HTML 리소스 제거를 확인했습니다.
- `docker build -t cobweb-app:local .`: 성공.
- 개발·운영 Compose `config --quiet`: 성공.
- GitHub Actions 원격 실행, 운영 서버 기동 및 외부 공개는 수행하지 않았습니다.
- 메일 실제 발송 및 OAuth 제공자 실계정 로그인은 검증하지 않았습니다.

## 추가 결함 검증 및 수정

2026-09-17 추가 회귀 검증에서 삭제 계정 토큰의 500 오류, 토큰 subject와 현재 계정 불일치,
같은 초 refresh token 중복, 로그인 실패 기록 롤백, 정지 계정 재발급, null 테스트 입력의 500 오류를
실패 테스트로 재현한 뒤 수정했습니다. 출력 제한 초과는 정답 문자열과 분리해 처리하고,
숫자 매개변수 형식 오류는 400으로 응답하도록 수정했습니다. 제품 기능은 추가하지 않았습니다.

이후 Strix 프롬프트를 분석해 Claude Code 읽기 전용 검토를 실행했습니다. 독립 테스트로 인증 만료,
재설정 토큰 동시 재사용, 채점 공용 스레드 풀 정체, 감사 로그 IP 위조를 수정했습니다.
정적 제안을 모두 확정 취약점으로 간주하지 않았으며 자세한 판정은 `docs/security/REVIEW.md`에 있습니다.

직접 추가 검토에서는 로그인과 재설정의 경합으로 비밀번호가 과거 값으로 돌아가는 문제,
동시 로그인 실패의 DB 교착, 타 요청의 이메일 인증 상태를 이용한 가입, 긴 User-Agent로 인한
감사 저장 실패를 재현하고 수정했습니다. 설명 주석과 네 개의 회귀 테스트를 추가했습니다.
회원가입 요청에는 기존 인증 코드인 `verificationCode`가 필요하며 통합된 가입 화면에도 반영했습니다.
재현 전제·수정 근거·남은 검증 공백은 `docs/security/REVIEW.md`에 있습니다.

## 운영 전 남은 사항

- 기존 DB는 백업/구조 비교 후 별도 마이그레이션해야 합니다. 자동 baseline/데이터 정리는 하지 않습니다.
- 도메인·TLS·실제 메일/OAuth 자격 증명 및 콜백 등록, 백업/복구·부하 테스트가 필요합니다.
- 공개 채점은 전용 격리 워커 인프라가 필요합니다. 현재 Compose worker는 Docker 소켓에 접근합니다.
- 시간 제한은 초 단위 CPU 제한과 컨테이너 준비 시간을 포함한 wall timeout입니다.
  엄밀한 밀리초 계측과 최대 메모리 사용량 측정은 미구현입니다.
- Python 3.12와 문자열 비교/체인 레이저 함수 형식만 지원합니다. 다중 언어·사용자 지정 검사기는 미구현입니다.
- 랭크 계산은 임시 정책입니다. 문제 본문은 안전한 일반 텍스트로 표시하며 Markdown 렌더러는 없습니다.
- 작성 화면은 예제/테스트 1개 입력만 지원합니다(API는 여러 개 지원).

시작 명령, API 목록과 상세 주의사항은 README.md를 참고하세요.

## 기존 계획 중 아직 구현하지 않은 항목

- 그룹 생성·가입·역할 관리 API와 가입 승인 화면. GROUP 문제 조회 권한 검사는 별도로 구현되어 있습니다.
- 태그/좋아요 관리 API와 태그·난이도 검색 화면.
- OpenAPI 문서 자동화, 운영 로그 수집·오류 추적.
- `terms.md`의 운영자·연락처·시행일 확정 및 개인정보 처리방침 준비.

## 중복 파일 정리 기준

- 화면은 `src/main/resources/static/index.html` 하나입니다. 기존 `/app.html` 주소는 같은 화면으로 내부 전달합니다.
- 스키마는 `src/main/resources/db/migration/`만 수정 기준으로 사용합니다. 이미 적용된 V1/V2 파일은 변경하지 않았습니다.
- 체인 레이저 시드는 `src/main/resources/demo/chain-laser.sql` 하나이며 `--demo.seed=true`로 실행합니다.
- 확정 보안 검토는 `docs/security/REVIEW.md`, 실행/API 안내는 `README.md`에 둡니다.
- 주석뿐인 구형 `compose.yaml`은 보관했습니다. 실제 개발·운영 구성은 역할이 달라 각각 유지합니다.
- 이전 원본은 `.local/archive/consolidation-2026-09-17/`에서 복구할 수 있습니다. 해당 폴더와 원시 검토 출력은 Git에 포함하지 않습니다.
